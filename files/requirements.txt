flask==3.0.3
pytest==8.2.0

# Intentionally pinned to an old version with a known CVE
# (CVE-2018-18074: Authorization header leak on cross-domain redirect).
# Not used by the app itself — included only so Snyk/Trivy have a real
# vulnerability to catch during the pipeline demo. See README for details.
requests==2.19.1
